The sum of a list of floats (L), which results in a float:
    Let r be 0.
    For each element in L, let r be r plus the element.
    The result is r.

To run:
    Let L be a list of floats containing 1, 2, and 3.5.
    print The sum of L.
